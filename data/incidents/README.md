# Synthetic Incident Dataset

The evaluation set is synthetic and versioned. Do not modify existing cases after a baseline measurement without recording a dataset-version change.

Each case should eventually include:
- incident narrative
- timestamped logs
- metrics snapshots
- deployment/config changes
- expected root cause
- distractor evidence
- difficulty label

The same fixed cases must be used for baseline and advanced comparisons.
